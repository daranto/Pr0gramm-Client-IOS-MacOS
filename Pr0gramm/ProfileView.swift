// ProfileView.swift
import SwiftUI

struct ProfileView: View {
    var body: some View {
        NavigationStack {
            Text("Profil (Noch nicht implementiert)")
                .navigationTitle("Profil")
        }
    }
}
#Preview { ProfileView() }
