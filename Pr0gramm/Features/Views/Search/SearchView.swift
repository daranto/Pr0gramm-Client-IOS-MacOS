import SwiftUI

struct SearchView: View {
    var body: some View {
        NavigationStack {
            Text("Suche (Noch nicht implementiert)")
                .navigationTitle("Suche")
        }
    }
}
#Preview { SearchView() }
